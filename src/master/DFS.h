#ifndef MINNIE_DFS_H
#define MINNIE_DFS_H

#include "hdfs.h"

class DFS
{
	private:
	public:
		DFS();
		~DFS();
		/*
			inspectDirectoryStructure(root dir);
			splitIntoMaps(all chunks);
			getDFSNode(chunk);
		*/
};

#endif
