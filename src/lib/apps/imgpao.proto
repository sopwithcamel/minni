package imagepao;

option optimize_for = SPEED;

message Neighbor {
    required string key = 1;
    required uint64 distance = 2;
}

message pao {
    required string key = 1;
    repeated Neighbor neighbors = 2;
}
